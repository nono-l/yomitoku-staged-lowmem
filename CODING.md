# コーディング方針

出典は [showroom-follow-sync](https://github.com/nono-l/showroom-follow-sync) の「コーディング方針（何も知らない AI 向け）」。
このリポジトリ向けに、部品の境界だけ足している。

コードを読む相手は「昨日の会話を覚えていない AI」だと仮定する。

## コメントに書くこと

- 何をしているかより、なぜそうしているかを書く。
- 関数・ファイルの先頭で、役割・やらないこと・前提・失敗して捨てた案を短く書く。
- 一見不思議な行（検出と認識を同じプロセスに載せない、torch のスレッドを 1 にする、など）には、制約か事故の理由を残す。
- 名前で分かる処理にはコメントを付けない。
- コメントは日本語。

## コメントに書かないこと

- コードを言い換えただけの説明
- 将来やるかもしれない機能の予告
- きさらぎ商店街や特定アプリの思い出。この部品は店を知らない

## 変更するとき

- 意図が変わったら、先にコメントを直してからコードを直す。
- コメントとコードが食い違っているなら、コメントが古い。コードに合わせてコメントを直す。
- 新しい制約を踏んだら、その場に一行で理由を残す。Issue やチャットにしか書かないと、次の AI は見えない。
- 書き換えは提出前に実行する。緑になるまで既定の入口を動かさない。

## この部品固有

- スキーマに分野語（店、伝票、診断）を入れない。
- ネット本体（DBNet / PARSeq の重み）は手書きで作り直さない。使っている前後処理と字表だけ専用に残す。
- 作業台に検出器と認識器を同時に広げない。
- 既定経路は yomitoku を import しない。比較用と export だけ残す。
- タイル経路（目録・タイル検出・足し）は既定に入れない。run_staged.sh は検出・認識・組み立てだけ。
