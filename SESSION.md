# 次のセッションへ

このファイルは、何も知らない AI / 人がこの部品を壊さずに続けるためのメモ。
コメント方針は [CODING.md](CODING.md)。元の型は [showroom-follow-sync](https://github.com/nono-l/showroom-follow-sync) と [onryo-ring の SESSION.md](https://github.com/nono-l/onryo-ring/blob/main/SESSION.md)。
「何をしているか」ではなく **なぜそうしているか・やると壊れること** を書く。

このセッション（2026-09-20）時点の動く面は、YomiToku を二プロセスに分けた切り出しだけである。
ONNX 化もハッシュ別置きもまだ無い。次は契約の文書化（P0a）と試験一枚の期待値（T0）と、重みを載せない組み立て（C1）。

---

## これは何か

画素から、文書として読める活字を、箱と信頼度と読み順つきで取り出す部品。
分野は知らない。設定資料でも伝票でも銘板でも、主キーが印刷文字なら同じ。

きさらぎ駅前商店街（https://cc0-kisaragi.grok.me/ ）は試験片である。製品の顔にしない。
Jev は後段の判定器であり、この部品に埋め込まない。

リポジトリ: https://github.com/nono-l/yomitoku-staged-lowmem

---

## 絶対に戻さないこと

1. **検出と認識を同じプロセスに載せない。** 約 2GiB・swap なしで、同時初期化は 137 になる。フル `yomitoku` CLI も同じ理由で既定にしない。
2. **分野語をスキーマに入れない。** 店名・伝票項目・診断名は上の組み立てである。
3. **DBNet / PARSeq を今夜書き直さない。** 書き換えるのは導線と凍結グラフ。ネットの自作は別プロジェクト。
4. **実行のたびに Hugging Face の「今の最新」を黙って引かない。** 別置きハッシュへ進む。今はまだ YomiToku ローダ。
5. **提出してから動かす、にしない。** 書のあとに証。証が緑になるまで既定もコミット対象の入口も動かさない。
6. **試験片を本体の名前にしない。** リポジトリ名の staged-lowmem は実行制約の記録。商店街専用器に改名しない。

---

## 今動いている面（切 C0）

入口は `run_staged.sh`。

1. `stage_detect.py` … `TextDetector` だけ。`points.json` を書いて終了する。
2. 別プロセスで `stage_recognize.py` … `parseq-tiny-dynw-v5` だけ。箱を 6 個ずつ。`ocr.json`。

実測: `https://cc0-kisaragi.grok.me/settei/21/card.jpg`（800×600）で検出 37 箱。
本文側に `中華料理店「夜光餃子座」パートタイマー制服資料` が残った。
低スコアの数字断片はごみ。本文が残ることが合格で、分類の正誤ではない。

YomiToku 0.15.0 はビルド／現行実行の依存。日常の最終形ではない。
宣言依存のうち PDF・表・レイアウト・reportlab は、この入口から踏まない。

---

## 互い違いの順（次から）

系統: 切＝今動く面を薄くする / 書＝契約と凍結 / 証＝同じ絵で本文が残るか。

| 順 | 系統 | ID | 状態 |
|---|---|---|---|
| 1 | 切 | C0 | 済（このリポジトリの入口） |
| 2 | 書 | P0a | 次。いまの JSON を `ocr_raw/v0` として書く |
| 3 | 証 | T0 | 試験一枚と期待文字列を固定する |
| 4 | 切 | C1 | 重み無し `stage_assemble.py`（読み順と noise だけ） |
| 5 | 書 | P0b | 組み立てを `document_ocr/v1` と名付ける |
| 6 | 証 | T1 | v0 と v1 で本文が消えていない |
| 7 | 書 | P1a | 現行重みをハッシュ付き別置き。ローダはまだ YomiToku |
| 8 | 切 | C2 | 別置きが無ければ失敗する。黙って最新を引かない |
| 9 | 書 | P1b | 検出だけ ONNX。既定はまだ旧検出 |
| 10 | 証 | T2 | 旧検出と ONNX 検出の箱 |
| 11 | 切 | C3 | 検出の既定を ONNX |
| 12 | 書 | P1c | 認識を ONNX。既定はまだ旧認識 |
| 13 | 証 | T3 | 旧認識と ONNX 認識の活字 |
| 14 | 切 | C4 | 認識の既定を ONNX。実行時から torch / YomiToku を外す |
| 15 | 証 | T4 | C0 と同じ絵で v1 本文が残る |
| 16 | 書 | P2 | 配布物をグラフ・charset・スキーマ・ランナーだけにする |

規則: 書のあとに証。切の既定入口は常に一つ。載せ替えは証が緑のときだけ。

---

## ファイルを触るとき

- 方針は `CODING.md`。セッションの継続はこのファイル。
- 入口を増やすな。増やすなら `run_staged.sh` が呼ぶ段を足す。
- 新しい段は重みを同時に二つ載せない。
- コメントは日本語。なぜ・やると壊れること。
- 意図が変わったらコメントが先。

## 既知の粗

- 短辺 600px は YomiToku 推奨 720 未満。試験ではそれでも店名は残った。
- tiny 認識は見出しの先頭が欠けることがある（`クター|`）。合格は固有名が残ること。
- `results/` と画像は git に入れない。
- サンドボックスは RAM 1.9GiB で、site-packages がターンで消えることがある。再現は別置き後に強くなる。
