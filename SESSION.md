# 次のセッションへ

2026-09-22: 空き定義にインク比を足した。画像を渡すと min_ink 0.02。
余白は落ちる。箱が無くインクがある区画だけ残る。タイル辺 320 は変えない。

2026-09-22: 空区画が残る紙で三段を実測。既定タイル 320。

| 紙 | 初回箱 | 空タイル | タイル箱 | 足し後 |
|---|---|---|---|---|
| 10 | 18 | 2 | 0 | 18 |
| 20 | 24 | 2 | 0 | 24 |
| 22 | 20 | 1 | — | — |

10 の空タイルは右端 160×320 と 160×280。余白。箱は増えなかった。
既定経路とタイル辺 320 は変えない。

2026-09-22: settei21 800×600 で三段を実測。既定タイル 320。
初回箱 36、空タイル 0、タイル箱 0、足し後 36。この紙では足す箱が無い。
タイル 160 なら空タイルは 7。既定は 320 のまま。

2026-09-22: 初回の箱を残し、重ならないタイル箱だけ足す。IoU 0.5。既定経路には入れない。

2026-09-22: 空いたタイルを切って、既存の検出を一枚ずつ別プロセスで呼ぶ入口を足した。
座標は元画像へ戻す。run_staged の既定には入れない。既定 1280 は触らない。

2026-09-22: 空いた区画の目録を切った。検出器は載せない。run_staged の既定には入れない。

2026-09-22: README の初見を短くした。clone の容量、pip 2 個、2GiB、BMP、出口の 3 ファイル。
`--shortest` は README に載せない。

2026-09-21: 最新 main に戻して 1280 を見た。既定は 1280 のまま。

| shortest | pred | 箱 | remain |
|---|---|---|---|
| 1280 | 1184×1600 | 36 | 残る |
| 960 | 960×1280 | 30 | 残る |
| 640 | 640×832 | 26 | 残る |

細い活字の紙では 1280 が要る可能がある。既定を下げない。`--shortest` は測定用。

---

## 絶対に戻さないこと

1. 検出と認識を同じプロセスに載せない。
2. AR 展開 ONNX を既定にしない。
3. 検出の既定を torch に戻さない。
4. 認識の既定を torch / yomitoku に戻さない。
5. 既定の検出・認識が yomitoku を import しないことを戻さない。
6. 結合後の実体を git に載せない。50MB 以上の単一ファイルを載せない。
7. 既定 verify に safetensors を強制しない。
8. comparison/ の外で yomitoku / torch を import しない。
9. 既定に pyclipper を戻さない。
10. 既定経路に opencv-python を戻さない。画素は cvsurf。
11. この部品に場面語を混ぜない。
12. README を作業台に戻さない。SESSION を消さない。
13. 検出の推論と箱出しを同じプロセスに戻さない。
14. 認識の encoder と decoder を同じプロセスに戻さない。
15. 組み立ての 0.25 を黙って下げない。変えるなら版を上げる。

---

## 今動いている面

- 最新 main、cvsurf + ONNX
- `--shortest` は測定。既定 1280
- 空いた区画の目録は detect_tiles/v0
- タイル検出は stage_detect_on_tiles.py。既定経路ではない
- 箱の足しは stage_merge_points.py。初回優先
- settei21 ではタイル 320 で空区画 0
- 10/20 の空タイルは余白。箱は増えず
- 画像付き目録は min_ink 0.02 で余白を落とす

---

## 次

min_ink 付き目録を 10 番で実測する。余白タイルが 0 になるか見る。
既定経路とタイル辺 320 は変えない。
